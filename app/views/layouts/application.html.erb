<!DOCTYPE html>
<html>
  <head>
    <title>ShaIsmail</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_tag %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <%= render 'partials/navbar' %>
    <div class="container">
      <div class="mt-4">
        <%= render 'partials/alert' %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
